server {
    listen 80;
    server_name localhost;

    # Serve static files from the build output
    root /usr/share/nginx/html;
    index index.html index.htm;

    # Handle client-side routing (React Router)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: You might need to add proxy settings later
    # if your frontend needs to directly call the backend API server-side
    # location /api/ {
    #     proxy_pass http://backend:8086; # Assuming backend service name is 'backend'
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }
}
