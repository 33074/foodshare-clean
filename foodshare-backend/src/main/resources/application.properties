# Local development properties (will be overridden by Render env vars)
spring.datasource.url=jdbc:mysql://localhost:3306/foodshare_db
spring.datasource.username=foodshare_user
spring.datasource.password=FoodShareUser_123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT (Set in Render)
jwt.secret.key=${JWT_SECRET_KEY:ZThkMDdjN2U5YzYwYTJmN2E3ZDY1YjY3ZGRmNzU3ZjA5YzgwYjM4YjAyYjM3MjI4YzI4OTM5M2M5YjIzYWU4NQ==}

# Server Port
server.port=8086

# Mail (Set in Render)
spring.mail.host=${SPRING_MAIL_HOST:smtp.example.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:user}
spring.mail.password=${SPRING_MAIL_PASSWORD:pass}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${SPRING_MAIL_FROM:noreply@example.com}

# Google OAuth (Set in Render)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:google-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:google-client-secret}
spring.security.oauth2.client.registration.google.scope=openid, profile, email
